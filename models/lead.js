'use strict';

var LeadModel = {};

LeadModel.getAll = function(CampignId){
	//return an array with all the campigns
};

LeadModel.getById = function(CampignId, LeadId){
	console.log(CampignId);
};

LeadModel.getByMediaAndCampign = function(Media, CampignId) {

};

LeadModel.getByKeyVal = function(CampignId, Key, Val) {

};

exports.LeadModel = LeadModel;